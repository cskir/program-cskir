// @ts-nocheck 

export default function AccountPage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center space-y-2">
        <h1 className="text-2xl font-bold">Account page disabled</h1>
        <p className="text-sm text-gray-500">
          This demo dApp focuses on the Poll voting flow. The default template
          Account page has been intentionally disabled for deployment.
        </p>
      </div>
    </main>
  );
}

// 'use client'
// import { redirect } from 'next/navigation'
// import AccountFeatureIndex from '@/features/account/account-feature-index'

// export default function Page() {
//   return <AccountFeatureIndex redirect={redirect} />
// }
